---
interface Props { items: { q: string; a: string }[] }
const { items = [] } = Astro.props;
---
{items.length > 0 && (
  <section class="faq" id="faq">
    <h2>Questions people ask</h2>
    <dl>{items.map((f) => <><dt>{f.q}</dt><dd>{f.a}</dd></>)}</dl>
  </section>
)}
